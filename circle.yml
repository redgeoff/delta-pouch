# Doesn't actually work!
# node:
#  version: 6.9.2

# # Uncomment to test with CouchDB 2
# machine:
#   services:
#     - docker

dependencies:
  pre:
    - nvm install 6.9.2

    # # Uncomment to test with CouchDB 2
    # CouchDB 2
    # - docker run -d --name couchdb -p 5984:5984 -e COUCHDB_USER='admin' -e COUCHDB_PASSWORD='admin' redgeoff/couchdb

  override:
    - nvm use 6.9.2 && npm install

test:
  pre:
    - nvm use 6.9.2 && npm run jshint

  override:
    - nvm use 6.9.2 && npm run node-full-test
    - nvm use 6.9.2 && npm run browser-test-phantomjs
